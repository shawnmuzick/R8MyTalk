<!-- Contact Form -->
<section id="contact-section" style="display: none">
  <div class="contactButton-container">
    <h3 id="contactInfo">Contact Information</h3>
  </div>

  <!-- Textarea containers -->
  <div class="contactButton-container">
    <textarea
      name="message"
      id="fullName"
      placeholder="Enter your name (optional)"
      style="width: 30em; height: 60px; resize: none"
    ></textarea>
  </div>
  <div class="contactButton-container">
    <textarea
      name="message"
      id="phoneNumber"
      placeholder="Enter your phone number (optional)"
      style="width: 30em; height: 60px; resize: none"
    ></textarea>
  </div>
  <div class="contactButton-container">
    <textarea
      name="message"
      id="email"
      placeholder="Enter your email (required) "
      style="width: 30em; height: 60px; resize: none"
    ></textarea>
  </div>

  <div class="contactButton-container">
    <textarea
      name="message"
      id="role"
      placeholder="What is your role? (required) "
      style="width: 30em; height: 60px; resize: none"
    ></textarea>
  </div>

  <!-- Button containers -->
  <div class="contactButton-container">
    <button type="button" class="button primary" id="contact-submit-btn">Submit</button>
  </div>
</section>
